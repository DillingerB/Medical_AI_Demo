use npm start in terminal to start the website, your data will be saved in the .sql file I will push, or have pushed. remove the file, add your password/username in the db file.
then you will have access to the mysql database

FIRST: put this into the terminal then run npm start
npm install dotenv express bcrypt mysql2
